mod document;
mod input;
mod mark;
mod node;
mod position;
mod schema;
mod selection;
mod slice;
mod state;
mod view;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
